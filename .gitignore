# Build output
target/

# IDE
.idea/
*.iml
.vscode/

# OpenSSL serial files (regenerated by generate-certs.sh)
certs/*.srl

# Logs
*.log

# Note: demo certificates/keystores under */resources/certs/ are intentionally
# committed so the demo runs out of the box. Never do this with real keys.
